# goReport 后端开发环境 Dockerfile
FROM golang:1.24-alpine

# 安装依赖
RUN apk add --no-cache git gcc musl-dev

# 设置工作目录
WORKDIR /app

# Use a faster Go proxy for dependency downloads.
ENV GOPROXY=https://goproxy.cn,direct

# 复制 go.mod 和 go.sum
COPY go.mod go.sum ./

# 下载依赖
RUN go mod download

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 8085

# 使用 go run 启动（无热重载）
CMD ["go", "run", "./cmd/server/main.go"]
