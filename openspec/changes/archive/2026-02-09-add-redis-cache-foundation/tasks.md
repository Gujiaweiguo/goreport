## 1. Implementation
- [x] 1.1 在配置层增加缓存开关、Redis 连接参数和默认 TTL。
- [x] 1.2 实现缓存抽象接口（`redis` + `noop`），保证 Redis 不可用时可降级。
- [x] 1.3 为数据源元数据查询（tables/fields）接入缓存读取与回填。
- [x] 1.4 为报表预览数据查询接入缓存读取与回填（含参数哈希键）。
- [x] 1.5 实现缓存失效：数据源更新/删除、报表保存后清理关联键。
- [x] 1.6 增加缓存观测：命中率、失败计数、降级日志。
- [x] 1.7 增加单元测试与集成测试（命中、失效、降级三条主路径）。
- [x] 1.8 更新运行文档（本地启停、配置示例、故障排查）。

